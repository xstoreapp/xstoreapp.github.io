name: Deploy CI

on: 
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    name: Checkout
    steps:
      - uses: actions/checkout@v2
      - run: yarn
      - run: yarn global add gh-pages
      - run: yarn build
      - run: yarn export
      - run: gh-pages -d out/